<UserControl x:Class="AlgoTrading.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="16,16,16,8" Padding="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Dashboard"
                              Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                              FontWeight="Bold"/>
                    <TextBlock Text="Overview of your trading account and activities"
                              Style="{StaticResource MaterialDesignBody2TextBlock}"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- SignalR Connection Status -->
                    <Border Background="{DynamicResource MaterialDesignToolBarBackground}"
                           CornerRadius="16"
                           Padding="12,6"
                           Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="Red"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHubConnected}" Value="True">
                                                <Setter Property="Fill" Value="Green"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Margin="8,0,0,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Disconnected"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHubConnected}" Value="True">
                                                <Setter Property="Text" Value="Live"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Button Content="REFRESH"
                           Style="{StaticResource MaterialDesignOutlinedButton}"
                           Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="16,8,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Assets Card -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="20">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Total Assets"
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <materialDesign:PackIcon Kind="AccountBalance"
                                               Width="24" Height="24"
                                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                                               HorizontalAlignment="Right"/>
                    </Grid>
                    <TextBlock Text="{Binding TotalAssets, StringFormat=₩{0:N0}}"
                              Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                              FontWeight="Bold"
                              Margin="0,8,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Cash: "
                                  Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <TextBlock Text="{Binding Cash, StringFormat=₩{0:N0}}"
                                  Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Today's P&L Card -->
            <materialDesign:Card Grid.Column="1" Margin="0,0,8,0" Padding="20">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Today's P&amp;L"
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <materialDesign:PackIcon Kind="TrendingUp"
                                               Width="24" Height="24"
                                               Foreground="Green"
                                               HorizontalAlignment="Right"/>
                    </Grid>
                    <TextBlock Text="{Binding TodayPnL, StringFormat=₩{0:N0}}"
                              Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                              FontWeight="Bold"
                              Margin="0,8,0,0"
                              Foreground="{Binding TodayPnL, Converter={StaticResource IsPositiveConverter}}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding DayPnLPercentage, StringFormat={}{0:+0.00;-0.00;0.00}%}"
                                  Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                  Foreground="{Binding TodayPnL, Converter={StaticResource IsPositiveConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Active Strategies Card -->
            <materialDesign:Card Grid.Column="2" Margin="0,0,8,0" Padding="20">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Active Strategies"
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <materialDesign:PackIcon Kind="ChartLine"
                                               Width="24" Height="24"
                                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                                               HorizontalAlignment="Right"/>
                    </Grid>
                    <TextBlock Text="{Binding ActiveStrategiesCount}"
                              Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                              FontWeight="Bold"
                              Margin="0,8,0,0"/>
                    <TextBlock Text="Strategies running"
                              Style="{StaticResource MaterialDesignCaptionTextBlock}"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              Margin="0,8,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Open Positions Card -->
            <materialDesign:Card Grid.Column="3" Padding="20">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="Open Positions"
                                  Style="{StaticResource MaterialDesignBody2TextBlock}"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <materialDesign:PackIcon Kind="ViewList"
                                               Width="24" Height="24"
                                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                                               HorizontalAlignment="Right"/>
                    </Grid>
                    <TextBlock Text="{Binding OpenPositionsCount}"
                              Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                              FontWeight="Bold"
                              Margin="0,8,0,0"/>
                    <TextBlock Text="Active holdings"
                              Style="{StaticResource MaterialDesignCaptionTextBlock}"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              Margin="0,8,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="16,8,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Recent Positions -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource MaterialDesignToolBarBackground}" Padding="20,16">
                        <TextBlock Text="Recent Positions"
                                  Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                  FontWeight="SemiBold"/>
                    </Border>

                    <DataGrid Grid.Row="1"
                             ItemsSource="{Binding RecentPositions}"
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             IsReadOnly="True"
                             SelectionMode="Single"
                             materialDesign:DataGridAssist.CellPadding="12"
                             materialDesign:DataGridAssist.ColumnHeaderPadding="12">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Stock"
                                               Binding="{Binding StockCode}"
                                               Width="100"/>

                            <DataGridTextColumn Header="Name"
                                               Binding="{Binding StockName}"
                                               Width="150"/>

                            <DataGridTextColumn Header="Qty"
                                               Binding="{Binding Quantity, StringFormat=N0}"
                                               Width="80"/>

                            <DataGridTextColumn Header="Avg Price"
                                               Binding="{Binding AveragePrice, StringFormat=N0}"
                                               Width="110"/>

                            <DataGridTextColumn Header="Current"
                                               Binding="{Binding CurrentPrice, StringFormat=N0}"
                                               Width="110"/>

                            <DataGridTextColumn Header="P&amp;L"
                                               Binding="{Binding ProfitLoss, StringFormat=N0}"
                                               Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsProfit}" Value="True">
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsLoss}" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="P&amp;L %"
                                               Binding="{Binding ProfitLossPercentage, StringFormat=N2}"
                                               Width="90">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsProfit}" Value="True">
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsLoss}" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </materialDesign:Card>

            <!-- Recent Orders -->
            <materialDesign:Card Grid.Column="1" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource MaterialDesignToolBarBackground}" Padding="20,16">
                        <TextBlock Text="Recent Orders"
                                  Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                  FontWeight="SemiBold"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding RecentOrders}" Margin="16">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}"
                                           BorderThickness="0,0,0,1"
                                           Padding="0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <materialDesign:PackIcon Grid.Column="0"
                                                                   Kind="ArrowUp"
                                                                   Width="20" Height="20"
                                                                   Foreground="Red"
                                                                   VerticalAlignment="Top"
                                                                   Margin="0,2,12,0">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsBuy}" Value="True">
                                                                <Setter Property="Kind" Value="ArrowDown"/>
                                                                <Setter Property="Foreground" Value="Green"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>

                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding StockCode, Mode=OneWay}"
                                                          Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                          FontWeight="SemiBold"/>
                                                <TextBlock Margin="0,4,0,0">
                                                    <Run Text="{Binding Side, Mode=OneWay}"/>
                                                    <Run Text="{Binding Quantity, StringFormat=N0, Mode=OneWay}"/>
                                                    <Run Text="@"/>
                                                    <Run Text="{Binding Price, StringFormat=N0, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock Text="{Binding Status, Mode=OneWay}"
                                                          Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                          Margin="0,4,0,0"/>
                                                <TextBlock Text="{Binding CreatedAt, StringFormat=HH:mm:ss, Mode=OneWay}"
                                                          Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                          Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Busy Indicator -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                           IsIndeterminate="True"
                           Width="60" Height="60"/>
                <TextBlock Text="{Binding BusyMessage}"
                          Foreground="White"
                          FontSize="16"
                          Margin="0,16,0,0"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
